import * as React from 'react';

export interface CreatePostPageProps {
}

export default function PostList (props: CreatePostPageProps) {
  return (
    <div>
      <h2>This is Create Post Page</h2>
    </div>
  );
}
